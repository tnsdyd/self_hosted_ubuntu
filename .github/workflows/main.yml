name: hello-world
on: push
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: my-step
        run: ls
      
      - name: Checkout
        uses: actions/checkout@v2
    
      - name: c
        run: ls
