name: hello-world
on: push
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - name: my-step
        run: ls
      
      - name: Checkout
        uses: actions/checkout@v3
      - run: sh test.sh
      
      - name: check2
        uses: actions/upload-artifact@v2
      
